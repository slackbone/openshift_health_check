---
# OpenShift Health Check Inventory
# This inventory file defines the hosts where the health check will be executed

all:
  children:
    localhost:
      hosts:
        localhost:
          ansible_connection: local
          ansible_python_interpreter: "{{ ansible_playbook_python }}"
          
    # Example for remote execution (uncomment and modify as needed)
    # openshift_clusters:
    #   hosts:
    #     cluster1:
    #       ansible_host: bastion.example.com
    #       ansible_user: admin
    #       ansible_ssh_private_key_file: ~/.ssh/openshift_key
    #       openshift_cluster_url: "https://api.cluster1.example.com:6443"
    #       openshift_token: "your-token-here"
    #       
    #     cluster2:
    #       ansible_host: bastion2.example.com
    #       ansible_user: admin
    #       ansible_ssh_private_key_file: ~/.ssh/openshift_key2
    #       openshift_cluster_url: "https://api.cluster2.example.com:6443"
    #       openshift_token: "your-token-here-2"
